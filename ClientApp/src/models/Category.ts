type Category = {
    id: string | null;
    name: string;
    limit: number;
    icon: string;
    description: string;
    visible: boolean;
    createdAt: string | null;
};

export default Category;
