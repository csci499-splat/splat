using splat.Models;
using System;
using System.Linq;
using Xunit;

namespace splat.Tests
{
    public class TotalReportsTest
    {
        [Fact]
        public void GetTotalWeights_SumOfWeights()
        {

        }

/*       private IQueryable<Pickup> GetTestPickups()
        {
            IQueryable<Pickup> pickups =
            {
                new Pickup
                {
                    Id = Guid.NewGuid(),
                    Weight = 25.5,
                    PickupStatus = PickupStatus.DISBURSED,
                    PickupTime = new DateTime(2020,05,05, 6, 22),
                    CanceledTime = null,
                    StudentInfo = new Student
                    {
                        StudentId = "1111111",
                        Age = 22,
                        OnMealPlan = false
                    },
                    SubmittedAt = new DateTime()

                },
                new Pickup { }
            }
        }*/
    }
}
